
#include <LiquidCrystal.h>

String Texto = "X"
LiquisCrystal lcd (12, 11, 5, 4, 3, 2);

void setup() {
  lcd.begin(16, 2);
}

void loop () {
int linha = 0;
int coluna = 0;
  for (linha; linha <=1; linha++){
    for (coluna, coluna <= 16; coluna++){
      lod.setCursor(coluna, linha);
      lod.print ("X");
      delay{500}
     Icd.clear();
     }
     coluna = 0;
   }
   linha = 0;
   if(Texto = "X")
   {
      Texto = " ";
   }else
   {
   Texto = "X";
   }
}
